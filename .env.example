# Copia este archivo como ".env" y completa los valores reales.
# El archivo ".env" NUNCA debe subirse a un repositorio (agrégalo a .gitignore).

# Django
DJANGO_SECRET_KEY=cambia-esta-clave-por-una-generada-de-forma-segura
DJANGO_DEBUG=True

# Base de datos MySQL
DB_NAME=constructora
DB_USER=root
DB_PASSWORD=Inacap22.
DB_HOST=localhost
DB_PORT=3306
